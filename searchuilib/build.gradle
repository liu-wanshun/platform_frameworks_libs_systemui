plugins {
    id 'com.android.library'
}

android {
    namespace "com.android.app.search"
    sourceSets {
        main {
            java.srcDirs = ['src']
            manifest.srcFile 'AndroidManifest.xml'
        }
    }
}